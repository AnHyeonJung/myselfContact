<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_CONTACTS" /> <!-- 주소록 읽기 -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" /> <!-- 주소록 쓰기 -->

    <application
        android:name="com.myself.myselfContact.HiltApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Part3chapter7"
        tools:targetApi="31">
        <activity
            android:name="com.myself.myselfContact.MainActivity"
            android:exported="true">
        </activity>
        <activity android:name="com.myself.myselfContact.InputActivity"
            android:windowSoftInputMode="adjustResize">
        </activity>
        <activity
            android:name="com.myself.myselfContact.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>